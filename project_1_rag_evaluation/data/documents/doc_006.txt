A vector database is a specialized data store optimized for storing and querying high-dimensional embedding vectors. Instead of exact-match lookups, vector databases support approximate nearest neighbor search, which quickly finds the vectors most similar to a query vector even across millions or billions of entries. They are a common component in production RAG systems, though small projects can often use a simple in-memory array with cosine similarity instead.
