Fine-tuning is the process of taking a pretrained model and continuing its training on a smaller, task-specific dataset so that it becomes better suited to a particular domain or behavior. Full fine-tuning updates all of a model's parameters, while parameter-efficient methods such as LoRA (low-rank adaptation) only train a small number of additional parameters, dramatically reducing the compute and memory required while still achieving strong task performance.
