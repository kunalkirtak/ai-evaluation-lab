Embeddings are dense numerical vector representations of text, images, or other data, designed so that semantically similar items end up close together in vector space. Sentence embedding models such as those built on the transformer encoder architecture map a sentence to a fixed-length vector. Similarity between two pieces of text can then be measured using metrics like cosine similarity, which computes the cosine of the angle between two vectors.
