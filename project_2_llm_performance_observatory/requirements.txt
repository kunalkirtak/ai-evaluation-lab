sentence-transformers>=2.2.2
numpy>=1.24
matplotlib>=3.7
pytest>=7.4

# Optional - only needed if USE_REAL_MODE=true and Gemini is used
google-generativeai>=0.5.0

# Optional - only needed if ENABLE_LANGSMITH=true
langsmith>=0.1.0
